# The base URL for the backend API.
# During local development, this is typically not needed because we fallback to http://localhost:3000
# For production (if frontend and backend are not on the same origin), set the full URL here.
VITE_API_URL=http://localhost:3000/api
